# Static analysis.
# tree-sitter is pinned deliberately: Language.query() is deprecated in 0.24
# and removed in 0.25, and Query.captures() changed shape across that boundary.
tree-sitter==0.24.0
tree-sitter-python==0.23.6

# PyPI metadata lookups.
requests==2.32.3

# Manifest read/write (format-preserving TOML).
tomlkit==0.15.1

# Version constraint comparison (range-aware, not string equality).
packaging==24.2

# Structured LLM output validation.
pydantic==2.11.4

# Version resolution. Replaces asking the model to invent version numbers.
uv==0.12.5

# Dense PyPI retrieval index (build_index.py). Only needed for
# --pypi-fallback dense; the lexical matcher needs none of this.
langchain-huggingface==1.2.2
langchain-community==0.3.23
langchain-core==1.6.0
sentence-transformers==4.1.0
faiss-cpu==1.11.0

# Tests.
pytest==9.1.1
